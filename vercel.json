{
    "builds": [
        {
            "src": "api/app.py",
            "use": "@vercel/python",
            "config": {
                "includeFiles": [
                    "static/**"
                ]
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/app.py"
        }
    ]
}